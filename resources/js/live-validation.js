(()=>{const area=document.querySelector('#lyrics'),summary=document.querySelector('#validation-summary'),chars=document.querySelector('#character-count'),captions=document.querySelector('#caption-count');if(!area)return;const update=()=>{const lines=area.value.split('\n');const active=lines.filter(x=>x.trim());const bad=[];active.forEach((line,i)=>{const parts=line.split('|');if(parts.length!==3||!parts[0].trim()||!parts[1].trim()||!parts[2].trim())bad.push(i+1);});chars.textContent=String(area.value.length);captions.textContent=String(active.length);summary.textContent=bad.length?'Check line'+(bad.length>1?'s ':' ')+bad.join(', ')+'. Server validation will confirm details.':'Browser guidance found no row-shape issues. Server validation remains authoritative.';summary.classList.toggle('error',bad.length>0);};area.addEventListener('input',update);update();})();
