# Untimed text intake

Untimed TXT files are accepted for inspection. The browser displays non-empty source lines, line count, character count, and Unicode text without generating timestamps. Final SRT conversion remains blocked until valid `START | END | TEXT` timing is provided.

This release does not provide placeholder timing or media-assisted synchronization. Adobe Premiere Pro and other editors remain downstream consumers, not input-format requirements.
