# UI Quick Start

```bash
git clone https://github.com/ADBranches/txt-to-srt-converter.git
cd txt-to-srt-converter
chmod +x bin/install-requirements
bin/install-requirements
bin/serve-ui
```

Open `http://127.0.0.1:8080/`. Contributors use `bin/install-requirements --development`.
